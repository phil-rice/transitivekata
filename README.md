We want to be able to expressive transitive dependencies

For example if A < B  and B < C then A < C.

The file in transitive.txt holds the data. 

We want to be answer questions like 'is a human an animal'
